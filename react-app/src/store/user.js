// Constants


// Action Creators


// Selectors


// Thunks


// reducer


let initialState = {
  currentServer: {},
  session: {},
  errors: {}
}

export default function userReducer( state = initialState, action) {
  switch(action.type) {
    default:
      return state;
  }
}
